import logging

logging.basicConfig(level=logging.DEBUG)
error=logging.error("This is for error message")
info=logging.info("This is for info message")
debug=logging.debug("This is for debug message")
critical=logging.critical("This is for critical message")
logging.warning("This is for warning message")